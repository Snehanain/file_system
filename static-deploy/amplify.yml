version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing dependencies..."
    build:
      commands:
        - echo "Building static site..."
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
